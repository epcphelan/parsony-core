const {
  setParsony,
  setConfigs,
  setTemplates,
  getTemplatesDir,
  sendTemplateEmail,
  getDebugRecipient
} = require('./email');

module.exports = {
  setParsony,
  setConfigs,
  setTemplates,
  getTemplatesDir,
  sendTemplateEmail,
  getDebugRecipient
};